node_modules
**/node_modules
dev-vars.yml
.ansible-docker-config
ops/Ansible/.ansible-docker-config

# Build output (TypeScript, etc.)
**/dist

# Next.js build output and cache
.next
**/.next

# TypeScript/build output (packages/common, packages/middleware, etc.)
dist
**/dist
*.tsbuildinfo
**/*.tsbuildinfo

# Terraform
.terraform
terraform.tfstate
terraform.tfstate.backup
terraform.tfvars

# Personal in-development docs/notes that don't belong in the repo
ops/README.md