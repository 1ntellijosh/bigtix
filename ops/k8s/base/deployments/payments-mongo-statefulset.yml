# ------------------------------------------------------------------------------------------------
# payments-mongo-srv mongo-db k8s StatefulSet file for BigTix platform microservices E-Commerce project
# @since payments-srv-start--JP
# ------------------------------------------------------------------------------------------------

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: payments-mongo-dpl
  namespace: default
spec:
  serviceName: payments-mongo-headless
  replicas: 1
  selector:
    matchLabels:
      app: payments-mongo
  template:
    metadata:
      labels:
        app: payments-mongo
    spec:
      containers:
        - name: payments-mongo
          image: mongo
          volumeMounts:
            - name: data
              mountPath: /data/db
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        storageClassName: ebs-csi-gp2
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 2Gi
---
apiVersion: v1
kind: Service
metadata:
  name: payments-mongo-headless
  namespace: default
spec:
  clusterIP: None
  selector:
    app: payments-mongo
  ports:
    - name: db
      port: 27017
      targetPort: 27017
---

apiVersion: v1
kind: Service
metadata:
  name: payments-mongo-srv
spec:
  selector:
    app: payments-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017 # Default MongoDB port
      targetPort: 27017 # Default MongoDB port