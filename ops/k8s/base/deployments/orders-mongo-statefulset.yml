# ------------------------------------------------------------------------------------------------
# orders-mongo-srv mongo-db k8s StatefulSet file for BigTix platform microservices E-Commerce project
# @since orders-srv-start--JP
# ------------------------------------------------------------------------------------------------

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: orders-mongo-dpl
  namespace: default
spec:
  serviceName: orders-mongo-headless
  replicas: 1
  selector:
    matchLabels:
      app: orders-mongo
  template:
    metadata:
      labels:
        app: orders-mongo
    spec:
      containers:
        - name: orders-mongo
          image: mongo
          volumeMounts:
            - name: data
              mountPath: /data/db
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        storageClassName: ebs-csi-gp2
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 2Gi
---
apiVersion: v1
kind: Service
metadata:
  name: orders-mongo-headless
  namespace: default
spec:
  clusterIP: None
  selector:
    app: orders-mongo
  ports:
    - name: db
      port: 27017
      targetPort: 27017
---
apiVersion: v1
kind: Service
metadata:
  name: orders-mongo-srv
spec:
  selector:
    app: orders-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017 # Default MongoDB port
      targetPort: 27017 # Default MongoDB port